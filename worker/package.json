{
  "name": "better-sidebar-license-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "generate-tokens": "npx tsx scripts/generate-tokens.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241018.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
